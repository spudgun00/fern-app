{
  "name": "fern-app",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "Fern patient zone (authenticated app). Phase P0: foundation + adapter spine.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler dev",
    "deploy": "astro build && wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "demo:menopause": "node scripts/demo-walk/menopause.mjs",
    "demo:weight": "node scripts/demo-walk/weight.mjs"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.0.1",
    "@supabase/ssr": "^0.12.0",
    "@supabase/supabase-js": "^2.108.2",
    "astro": "^7.0.3"
  },
  "devDependencies": {
    "playwright": "^1.48.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9",
    "wrangler": "^4.105.0"
  }
}
